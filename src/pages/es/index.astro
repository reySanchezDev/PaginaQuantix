---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Container from "../../components/ui/Container.astro";
import Button from "../../components/ui/Button.astro";
import { products } from "../../data/products";

const positioning = [
  "Sistemas operativos",
  "Plataformas SaaS",
  "Integraciones",
  "Reportería & BI",
  "Automatización",
];

const operations = [
  {
    title: "Claridad operativa",
    text: "Software diseñado alrededor de flujos reales, roles, aprobaciones, movimiento de datos y ejecución diaria.",
  },
  {
    title: "Ejecución confiable",
    text: "Plataformas y herramientas para equipos que necesitan sistemas dependibles, no interfaces decorativas.",
  },
  {
    title: "Control escalable",
    text: "Integraciones, reportería y automatización pensadas para reducir trabajo manual mientras la operación crece.",
  },
];

const solutions = [
  "Sistemas de software operativo",
  "Plataformas SaaS",
  "Integraciones & APIs",
  "Reportería e inteligencia de negocio",
  "Automatización de procesos",
];

const workAreas = [
  "Operaciones de restaurantes",
  "Herramientas financieras",
  "Control de inventario",
  "Flujos de facturación",
  "Sistemas relacionados con POS",
  "Dashboards de reportería",
  "Herramientas de automatización",
];

const process = [
  {
    title: "Entender la operación",
    text: "Aclaramos flujos, restricciones, usuarios, datos y el resultado de negocio que el sistema debe apoyar.",
  },
  {
    title: "Diseñar el sistema",
    text: "Definimos la estructura del producto, integraciones, modelo de datos y ruta de implementación antes de construir.",
  },
  {
    title: "Construir e integrar",
    text: "Desarrollamos la plataforma, herramienta o automatización con uso real y mantenibilidad en mente.",
  },
  {
    title: "Acompañar la evolución",
    text: "Ayudamos a mejorar el sistema conforme cambian los requisitos, equipos y condiciones operativas.",
  },
];
---

<BaseLayout
  title="Sistemas de Software para Operaciones Reales de Negocio"
  description="Quantix Software diseña y construye plataformas digitales, herramientas operativas, integraciones, sistemas de reportería y flujos de automatización para empresas que necesitan claridad, control y ejecución escalable."
  lang="es"
>
  <main class="home-page">
    <section class="home-hero" aria-labelledby="home-hero-title">
      <Container class="home-hero__container">
        <div class="home-hero__content">
          <p class="home-kicker">Quantix Software</p>
          <h1 id="home-hero-title">Sistemas de software para operaciones reales de negocio</h1>
          <p class="home-hero__lead">
            Quantix Software diseña y construye plataformas digitales, herramientas operativas, integraciones, sistemas de reportería y flujos de automatización para empresas que necesitan claridad, control y ejecución escalable.
          </p>
          <div class="home-actions">
            <Button href="/es/contacto/" variant="primary" size="lg">Solicitar consulta</Button>
            <Button href="/es/soluciones/" variant="secondary" size="lg">Explorar soluciones</Button>
          </div>
        </div>

        <div class="home-system-panel" aria-label="Mapa de software operativo">
          <div class="system-panel__header">
            <span>Operaciones de negocio</span>
            <strong>Capa de sistemas</strong>
          </div>
          <div class="system-flow">
            <div class="system-node system-node--primary">
              <span>Operación</span>
              <strong>Flujos centrales</strong>
            </div>
            <div class="system-node">
              <span>Datos</span>
              <strong>Reportería</strong>
            </div>
            <div class="system-node">
              <span>APIs</span>
              <strong>Integraciones</strong>
            </div>
            <div class="system-node">
              <span>Equipos</span>
              <strong>Automatización</strong>
            </div>
          </div>
        </div>
      </Container>
    </section>

    <section class="positioning-strip" aria-label="Enfoques de Quantix Software">
      <Container>
        <ul>
          {positioning.map((item) => <li>{item}</li>)}
        </ul>
      </Container>
    </section>

    <section class="home-section" aria-labelledby="operations-title">
      <Container>
        <div class="section-heading section-heading--wide">
          <p class="section-eyebrow">Software operativo</p>
          <h2 id="operations-title">Construido para operaciones reales de negocio</h2>
          <p>
            Quantix Software se enfoca en software que ordena procesos, datos y ejecución: sistemas que los equipos pueden usar cada día para operar con menos fricción y más control.
          </p>
        </div>

        <div class="editorial-grid">
          {operations.map((item) => (
            <article class="editorial-card">
              <h3>{item.title}</h3>
              <p>{item.text}</p>
            </article>
          ))}
        </div>
      </Container>
    </section>

    <section class="home-section home-section--surface" aria-labelledby="solutions-title">
      <Container>
        <div class="section-split">
          <div class="section-heading">
            <p class="section-eyebrow">Soluciones</p>
            <h2 id="solutions-title">Capacidades de software para operación, datos y ejecución</h2>
            <p>
              Diseñamos sistemas que conectan flujos operativos con los datos, integraciones y automatización necesarios para ejecutarlos con confianza.
            </p>
            <div class="home-actions">
              <Button href="/es/soluciones/" variant="secondary">Explorar soluciones</Button>
            </div>
          </div>

          <div class="capability-list">
            {solutions.map((item) => (
              <a href="/es/soluciones/" class="capability-item">
                <span>{item}</span>
              </a>
            ))}
          </div>
        </div>
      </Container>
    </section>

    <section class="home-section" aria-labelledby="products-title">
      <Container>
        <div class="section-heading section-heading--wide">
          <p class="section-eyebrow">Productos</p>
          <h2 id="products-title">Productos de software publicados</h2>
          <p>
            Quantix Software construye y opera productos enfocados para flujos reales de negocio y finanzas.
          </p>
        </div>

        <div class="product-preview-grid">
          {products.map((product) => (
            <article class="product-preview-card">
              <div>
                <span class="product-status">{product.status.es}</span>
                <h3>{product.title.es}</h3>
                <p>{product.description.es}</p>
              </div>
              <ul>
                {product.features.es.map((feature) => <li>{feature}</li>)}
              </ul>
              <div class="card-actions">
                <Button href={product.href.es} variant="secondary">Ver producto</Button>
                {product.publicUrl && (
                  <Button href={product.publicUrl} target="_blank" variant="primary">Visitar sitio web</Button>
                )}
              </div>
            </article>
          ))}
        </div>
      </Container>
    </section>

    <section class="home-section home-section--surface" aria-labelledby="work-title">
      <Container>
        <div class="section-split section-split--reverse">
          <div class="work-panel">
            {workAreas.map((item) => <span>{item}</span>)}
          </div>

          <div class="section-heading">
            <p class="section-eyebrow">Experiencia</p>
            <h2 id="work-title">Experiencia en áreas de software operativo</h2>
            <p>
              Nuestro trabajo se organiza alrededor de los tipos de sistemas que las empresas necesitan para operar, controlar información y reducir trabajo repetitivo.
            </p>
            <div class="home-actions">
              <Button href="/es/trabajo/" variant="secondary">Ver áreas de trabajo</Button>
            </div>
          </div>
        </div>
      </Container>
    </section>

    <section class="home-section" aria-labelledby="process-title">
      <Container>
        <div class="section-heading section-heading--wide">
          <p class="section-eyebrow">Cómo trabajamos</p>
          <h2 id="process-title">Del contexto operativo al software en uso</h2>
        </div>

        <div class="process-grid">
          {process.map((step, index) => (
            <article class="process-card">
              <span>{String(index + 1).padStart(2, "0")}</span>
              <h3>{step.title}</h3>
              <p>{step.text}</p>
            </article>
          ))}
        </div>
      </Container>
    </section>

    <section class="home-section home-section--closing" aria-labelledby="closing-title">
      <Container>
        <div class="closing-panel">
          <div>
            <p class="section-eyebrow">Iniciar conversación</p>
            <h2 id="closing-title">Hablemos sobre tu proyecto de software</h2>
            <p>
              Trae el contexto operativo. Podemos ayudar a definir el sistema, integración o automatización que mejor encaja.
            </p>
          </div>
          <Button href="/es/contacto/" variant="primary" size="lg">Contactar a Quantix</Button>
        </div>
      </Container>
    </section>
  </main>
</BaseLayout>

<style>
  .home-page {
    background: var(--bg);
    color: var(--text);
  }

  .home-hero {
    padding: clamp(8rem, 13vw, 10.5rem) 0 clamp(4rem, 7vw, 6rem);
    border-bottom: 1px solid var(--border);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--primary-soft) 24%, var(--bg)) 0%, var(--bg) 72%);
  }

  :global(.home-hero__container) {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(22rem, 0.92fr);
    gap: clamp(2rem, 5vw, 5rem);
    align-items: center;
  }

  .home-hero__content,
  .section-heading,
  .home-system-panel,
  .closing-panel > div {
    min-width: 0;
  }

  .home-kicker,
  .section-eyebrow {
    margin: 0 0 var(--space-4);
    color: var(--primary);
    font-size: var(--text-sm);
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .home-hero h1 {
    max-width: 48rem;
    margin: 0;
    color: var(--text);
    font-size: clamp(2.75rem, 7vw, 5.4rem);
    font-weight: 800;
    line-height: 0.98;
    letter-spacing: -0.026em;
    text-wrap: balance;
    overflow-wrap: break-word;
  }

  .home-hero__lead {
    max-width: 46rem;
    margin: var(--space-6) 0 0;
    color: var(--text-muted);
    font-size: clamp(1.05rem, 2vw, 1.28rem);
    line-height: 1.72;
  }

  .home-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-8);
  }

  .home-system-panel {
    position: relative;
    overflow: hidden;
    min-height: 31rem;
    padding: var(--space-6);
    border: 1px solid color-mix(in srgb, var(--primary) 12%, var(--border));
    border-radius: var(--radius-lg);
    background:
      linear-gradient(145deg, color-mix(in srgb, var(--surface) 94%, var(--primary-soft)), var(--surface));
    box-shadow: var(--shadow-md);
  }

  .home-system-panel::before {
    content: "";
    position: absolute;
    inset: 4rem -18% auto;
    height: 1px;
    background: linear-gradient(90deg, transparent, color-mix(in srgb, var(--primary) 26%, transparent), transparent);
  }

  .system-panel__header {
    display: flex;
    justify-content: space-between;
    gap: var(--space-4);
    color: var(--text-muted);
    font-size: var(--text-sm);
  }

  .system-panel__header strong {
    color: var(--text);
  }

  .system-flow {
    display: grid;
    gap: var(--space-4);
    margin-top: var(--space-10);
  }

  .system-node {
    padding: var(--space-4);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: color-mix(in srgb, var(--surface) 90%, white);
  }

  .system-node--primary {
    border-color: color-mix(in srgb, var(--primary) 24%, var(--border));
    background: color-mix(in srgb, var(--primary-soft) 38%, var(--surface));
  }

  .system-node span {
    display: block;
    color: var(--text-muted);
    font-size: var(--text-sm);
    font-weight: 700;
  }

  .system-node strong {
    display: block;
    margin-top: var(--space-1);
    font-size: var(--text-xl);
    line-height: 1.25;
  }

  .positioning-strip {
    padding: var(--space-4) 0;
    border-bottom: 1px solid var(--border);
    background: var(--surface);
  }

  .positioning-strip ul {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    justify-content: center;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .positioning-strip li {
    padding: 0.5rem 0.8rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-full);
    color: var(--text-muted);
    font-size: var(--text-sm);
    font-weight: 700;
    background: var(--surface-2);
  }

  .home-section {
    padding: clamp(4rem, 7vw, 6.5rem) 0;
  }

  .home-section--surface {
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: var(--surface-2);
  }

  .section-heading {
    max-width: 42rem;
  }

  .section-heading--wide {
    max-width: 58rem;
    margin-bottom: var(--space-10);
  }

  .section-heading h2,
  .closing-panel h2 {
    margin: 0;
    color: var(--text);
    font-size: clamp(2rem, 4.4vw, 3.35rem);
    font-weight: 800;
    line-height: 1.08;
    letter-spacing: -0.018em;
    text-wrap: balance;
  }

  .section-heading p,
  .closing-panel p {
    margin: var(--space-4) 0 0;
    color: var(--text-muted);
    font-size: var(--text-lg);
    line-height: 1.7;
  }

  .editorial-grid,
  .product-preview-grid,
  .process-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-5);
  }

  .editorial-card,
  .product-preview-card,
  .process-card {
    min-height: 100%;
    padding: clamp(1.35rem, 3vw, 1.75rem);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
    box-shadow: var(--shadow-sm);
  }

  .editorial-card h3,
  .product-preview-card h3,
  .process-card h3 {
    margin: 0 0 var(--space-3);
    font-size: var(--text-xl);
    line-height: 1.25;
  }

  .editorial-card p,
  .product-preview-card p,
  .process-card p {
    margin: 0;
    color: var(--text-muted);
    line-height: 1.66;
  }

  .section-split {
    display: grid;
    grid-template-columns: minmax(0, 0.92fr) minmax(0, 1.08fr);
    gap: clamp(2rem, 5vw, 5rem);
    align-items: start;
  }

  .section-split--reverse {
    grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
  }

  .capability-list {
    display: grid;
    gap: var(--space-3);
  }

  .capability-item {
    display: flex;
    align-items: center;
    min-height: 4.4rem;
    padding: 0 var(--space-5);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: var(--surface);
    color: var(--text);
    font-weight: 800;
    text-decoration: none;
    box-shadow: var(--shadow-sm);
    transition: border-color 0.2s ease, color 0.2s ease, background-color 0.2s ease;
  }

  .capability-item:hover {
    border-color: var(--border-strong);
    color: var(--primary);
    background: color-mix(in srgb, var(--surface) 88%, var(--primary-soft));
  }

  .product-preview-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .product-preview-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }

  .product-status {
    display: inline-flex;
    margin-bottom: var(--space-4);
    padding: 0.32rem 0.62rem;
    border: 1px solid color-mix(in srgb, var(--primary) 14%, var(--border));
    border-radius: var(--radius-full);
    color: var(--primary);
    background: color-mix(in srgb, var(--primary-soft) 34%, var(--surface));
    font-size: var(--text-xs);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .product-preview-card ul {
    margin: 0;
    padding-left: 1.1rem;
    color: var(--text-muted);
    line-height: 1.7;
  }

  .product-preview-card li + li {
    margin-top: var(--space-2);
  }

  .card-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: auto;
  }

  .work-panel {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    align-content: flex-start;
    padding: clamp(1.5rem, 4vw, 2rem);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
    box-shadow: var(--shadow-sm);
  }

  .work-panel span {
    padding: 0.55rem 0.8rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-full);
    color: var(--text);
    background: var(--surface-2);
    font-size: var(--text-sm);
    font-weight: 700;
  }

  .process-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .process-card span {
    display: block;
    margin-bottom: var(--space-5);
    color: var(--primary);
    font-size: var(--text-sm);
    font-weight: 800;
  }

  .home-section--closing {
    padding-top: 0;
  }

  .closing-panel {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-8);
    padding: clamp(2rem, 5vw, 3rem);
    border: 1px solid color-mix(in srgb, var(--primary) 10%, var(--border));
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, color-mix(in srgb, var(--surface) 94%, var(--primary-soft)), var(--surface));
  }

  .closing-panel > div {
    max-width: 45rem;
  }

  @media (max-width: 980px) {
    :global(.home-hero__container),
    .section-split,
    .section-split--reverse {
      grid-template-columns: 1fr;
    }

    .home-system-panel {
      min-height: auto;
    }

    .editorial-grid,
    .process-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 680px) {
    .home-hero {
      padding-top: 7.5rem;
    }

    .home-hero h1 {
      font-size: clamp(2.05rem, 9.5vw, 2.65rem);
      line-height: 1.08;
      letter-spacing: -0.018em;
      text-wrap: wrap;
      max-width: 19rem;
    }

    .home-hero__content,
    .home-hero__lead,
    .home-actions,
    .home-system-panel {
      width: min(100%, calc(100vw - 2rem));
      max-width: calc(100vw - 2rem);
    }

    .home-system-panel {
      padding: var(--space-4);
    }

    .system-panel__header {
      flex-direction: column;
      gap: var(--space-1);
    }

    .home-actions,
    .card-actions,
    .closing-panel {
      flex-direction: column;
      align-items: stretch;
    }

    .home-actions :global(.btn),
    .card-actions :global(.btn),
    .closing-panel :global(.btn) {
      width: 100%;
      justify-content: center;
    }

    .editorial-grid,
    .product-preview-grid,
    .process-grid {
      grid-template-columns: 1fr;
    }

    .positioning-strip ul {
      justify-content: flex-start;
    }

    .section-heading h2,
    .closing-panel h2 {
      font-size: clamp(1.85rem, 9vw, 2.45rem);
    }
  }
</style>
