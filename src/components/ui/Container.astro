---
interface Props extends Omit<astroHTML.JSX.HTMLAttributes, "class"> {
  class?: string;
  narrow?: boolean;
}

const { class: className, narrow = false, ...attributes } = Astro.props as Props;
---

<div class:list={["container", { "container--narrow": narrow }, className]} {...attributes}>
  <slot />
</div>
